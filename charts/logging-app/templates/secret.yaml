apiVersion: v1
kind: Secret
metadata:
  name: splunk-secret
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "logging-app.labels" . | nindent 4 }}
type: Opaque
data:
  SPLUNK_TOKEN: {{ .Values.splunk.token | b64enc | quote }}
